{
  "name": "moomooinvest-dashboard",
  "private": true,
  "description": "Static DCA dashboard (dashboard/index.html) plus tiny serverless APIs (api/ticks.js, api/notes.js) that store checked-off buy rungs and per-rung notes in a small database so they sync across devices.",
  "dependencies": {
    "@vercel/kv": "^2.0.0"
  }
}
